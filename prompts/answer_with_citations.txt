You will receive:
- QUESTION
- CONTEXT: chunks with chunk_id + source + page + text

Task:
1) Answer the QUESTION using ONLY the CONTEXT.
2) Every factual claim MUST include at least one citation [chunk_id].
3) If the CONTEXT is insufficient, refuse.

Return ONLY:
- answer: string
- cited_chunk_ids: list[string]
- refusal: {is_refusal: bool, reason: string}
