# Failure cases

1) PDFs with poor text extraction (scans/layout-heavy) → missing evidence.
2) Tables and structured docs chunk badly with naive chunking.
3) Ambiguous questions may require multi-hop retrieval.
4) Confidence is heuristic without real calibration labels.
