app:
  environment: prod

api:
  reload: false
  request_timeout_s: 60

retrieval:
  hybrid:
    enabled: true
    fusion_method: rrf
    dense_k: 60
    bm25_k: 250
    rrf_k: 30
  rerank:
    enabled: false
